import pandas as pd

# Load data/test_run_data.csv (columns: Unit, Timestamp, TempF, RH_pct, Status)
# Print a summary of any rows where Status is 'FAULT', showing which unit and when the fault occurred, along with the temperature and humidity at that time.
